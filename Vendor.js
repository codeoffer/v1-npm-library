class Vendor {
    constructor(uuid, username, verified, profile_picture) {
        this.uuid = uuid;
        this.username = username;
        this.verified = verified;
        this.profile_picture = profile_picture;
    }
}

module.exports = Vendor